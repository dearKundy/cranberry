mysql:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/justbattle?characterEncoding=UTF-8
    username: root
    password: 123456

mybatis:
  mapper-locations: classpath:mybatis/mapper/*.xml

logging:
  level:
    com.kundy.justbattle.mapper: debug

